import styled from "styled-components"
import { Cores } from "../../cores"

export const ImagemFundo = styled.div`
    img{
        width: 98%;
        border-radius: 10px;
        margin: 1%;
        filter: grayscale(75%);
    }
    
`